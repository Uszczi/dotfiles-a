#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
PS1='[\u@\h \W]\$ '







# Bug with todoist app
export DESKTOPINTEGRATION=false

export PATH="/home/mat/path-utils:$PATH"

fish

