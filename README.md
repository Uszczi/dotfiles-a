### Very simple app for synchronizing configs files between computers.

### Instalation
`make install`

### Usage
`dotfiles copy` - copy files from disk to repo

`dotfiles paste` - copy files from repo to disk

